spring.config.import=file:/project/secrets/movieSecrets.properties
logging.level.org.springframework.security=${LOGGING_LEVEL}
spring.datasource.main.url=${DB_URL}
spring.datasource.main.username=${DB_USERNAME}
spring.datasource.main.password=${DB_PASSWORD}
spring.datasource.movieinfo.url=${MOVIEINFO_DB_URL}
spring.datasource.movieinfo.username=${MOVIEINFO_DB_USERNAME}
spring.datasource.movieinfo.password=${MOVIEINFO_DB_PASSWORD}
security.allowedOrigin=${ALLOWED_ORIGIN}
client.url=${CLIENT_URL}
email.host=${EMAIL_HOST}
email.port=${EMAIL_PORT}
email.emailUsername=${EMAIL_USERNAME}
email.emailPassword=${EMAIL_PASSWORD}
url.catalog=${CATALOG_URL}
omdb.apikey=${OMDB_APIKEY}